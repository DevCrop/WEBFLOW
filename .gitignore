# Credentials / secrets
.credentials*
.env
.env.*
*.pem
*.key

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo

# Node / build artifacts (in case future tooling lands here)
node_modules/
dist/
build/
.next/
.cache/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude local-only state (rules/skills tracked, runtime state ignored)
.claude/projects/
.claude/shell-snapshots/
.claude/todos/
.claude/sessions/
.claude/cache/
.claude/.credentials*
.claude/settings.local.json

# Brand assets — too large for GitHub (kept locally only)
.claude/docs/
